#!/bin/bash
# Простой скрипт для обновления сайта

# 1. Добавляем все изменения
git add .

# 2. Фиксируем с сообщением (по умолчанию "Update")
msg="Update site $(date)"
if [ -n "$1" ]; then
    msg="$1"
fi
git commit -m "$msg"

# 3. Отправляем на GitHub
git push origin main

echo "🚀 Изменения отправлены на GitHub! Сайт обновится через 1-2 минуты."
